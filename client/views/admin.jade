extends ./partials/layout.jade

block menu
    admin-menu

block user 
    .user
        .user__name
          div= user.name
          a.user__log-out(href="/logout") log out&nbsp
            i.fa.fa-sign-out(aria-hidden="true")
        img.user__avatar(src=user.avatar) 

block scripts
    script(src="/js/vendor.js" type="text/javascript")
    script(src="/js/admin.js" type="text/javascript")
